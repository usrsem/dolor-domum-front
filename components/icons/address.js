import React from "react";
import { Image } from "@chakra-ui/react";

const Address = (props) => {
  return (
    <Image src="/images/icons/address.svg" alt="icon" {...props} w={7} h={7} />
  );
};

export default Address;
